import { FilterDriversPipe } from './filter-drivers.pipe';

describe('FilterDriversPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDriversPipe();
    expect(pipe).toBeTruthy();
  });
});
